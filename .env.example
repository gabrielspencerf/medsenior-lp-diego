# Copie para `.env` na raiz do projeto.
# Somente 3 variáveis de configuração:

# Praça ativa da landing (`curitiba` ou `recife`)
VITE_MARKET=curitiba

# ID público do Typebot (slug do fluxo)
VITE_TYPEBOT_PUBLIC_ID=diegomedsenior-pe

# ID do Google Tag Manager
VITE_GTM_CONTAINER_ID=GTM-NQ825NPL
