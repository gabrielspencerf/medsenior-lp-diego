# ChatGPT / GPT — regras e contexto

## Resumo para colar em conversas novas

> Repositório: landing **React 19** (Vite, Tailwind v4, Motion, Lucide). UI da home em `src/pages/LandingPage.tsx`; rotas e analytics em `src/App.tsx`. Documentação em `docs/`: `README.md` (índice), `structure.md`, `design/design-system.md`, `copy.md`, `maintenance.md`, `performance.md`, `logs/fixes.md`. Assistentes: pasta `docs/ai/`.

## O que pedir ao modelo

- Sempre ancorar sugestões nos documentos acima quando forem mudanças de marca, tom ou layout.
- Não supor existência de `src/components` sem verificar o repositório.
- Para performance: seguir `docs/performance.md`.

## Limitações típicas

- O modelo pode não ver o estado atual do git; peça confirmação de trechos críticos ou use anexo de arquivo.
- Claims de saúde e registros ANS: não inventar; alinhar com copy aprovada e com `src/pages/LandingPage.tsx`.
